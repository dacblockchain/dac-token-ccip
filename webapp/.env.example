# Optional read-only RPC endpoints for the bridge UI. Copy to .env.local.
# Only VITE_* variables are exposed to the browser, so never put private keys here.
VITE_MAINNET_RPC_URL=
VITE_BSC_RPC_URL=
VITE_SEPOLIA_RPC_URL=
VITE_BSC_TESTNET_RPC_URL=
